/* ============================================
   CRIMEGPT 2.0 — DASHBOARD COMPONENTS
   ============================================
   Barrel exports for dashboard shared components.
   Role-specific views remain in Dashboard.tsx for now.
   ============================================ */

export { StatCard, CaseRow, ReadinessBar } from './SharedComponents';
